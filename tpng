#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$ROOT/.venv/bin/python"
if [ ! -x "$PY" ]; then
  echo "Virtualenv not found. Run ./install.sh first." >&2
  exit 1
fi
exec "$PY" -m transparent_png_lab "$@"
